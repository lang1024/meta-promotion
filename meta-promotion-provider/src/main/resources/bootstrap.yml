
#放置配置文件，配置文件不能放倒application.yml中，因为配置文件先于application.yml加载，bootstrap.yml先application文件加载
#spring:
#  application:
#    name: meta-promotion-provider    # 对应microservice-config-server所获取的配置文件的{application}
#  cloud:
#    config:
#      uri:                              #配置中心的服务
#      profile: dev                      # 指定profile，对应microservice-config-server所获取的配置文件中的{profile}
#      label: master                     # 指定git仓库的分支，对应microservice-config-server所获取的配置文件的{label}